name: Publish VS Code Extension

on:
  push:
    tags:
      - 'code-v*'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        working-directory: vibe-code
        run: npm install --legacy-peer-deps
      
      - name: Compile extension
        working-directory: vibe-code
        run: npm run compile
      
      - name: Package extension
        working-directory: vibe-code
        run: npx @vscode/vsce package --no-dependencies
      
      - name: Get version
        id: version
        run: echo "VERSION=$(node -p "require('./vibe-code/package.json').version")" >> $GITHUB_OUTPUT
      
      - name: Publish to VS Code Marketplace
        working-directory: vibe-code
        run: npx @vscode/vsce publish --no-dependencies -p ${{ secrets.VSCE_TOKEN }}
        continue-on-error: true
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: Vibe VS Code Extension v${{ steps.version.outputs.VERSION }}
          body: |
            ## 🚀 Vibe VS Code Extension v${{ steps.version.outputs.VERSION }}
            
            **Install from VS Code Marketplace:**
            Search "Vibe VS Code" in VS Code Extensions
            
            **Or download VSIX below and install manually:**
            1. Download `vibe-vscode-${{ steps.version.outputs.VERSION }}.vsix`
            2. In VS Code: Extensions → ⋯ → Install from VSIX
            
            ### ✨ Features:
            - 🤖 **6 AI Modes**: Code, Architect, Debug, Test, Review, Explain
            - 🌐 **Multi-Provider**: OpenRouter + MegaLLM support
            - 📁 **Direct File Operations**: Create/edit files from chat
            - 💬 **In-Editor Chat**: AI assistance without leaving VS Code
            - 🎨 **5 Visual Themes**: Customize your experience
            - ⌨️ **Keyboard Shortcuts**: Quick mode switching
            
            ### 🎯 Quick Start:
            1. Install extension
            2. Press `Ctrl+Shift+P` → "Vibe: Open Chat"
            3. Start coding with AI!
            
            See [README](https://github.com/mk-knight23/vibe/blob/main/vibe-code/readme.md) for details.
          files: vibe-code/*.vsix
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
